{
	"name": "example", // Layer name
	"waitForComplete": true, // Parse after competion
	"cancelOnKey": "", // key returns to default layer
	"prevLayerOnKey": "", // key goes to previous layer
	"completeOnKey": "", // Completes once pressed
	"onKey": [], // actions when any key is pressed
	"onPrintableKey": [], // actions when a printable key is pressed
	"onNonPrintableKey": [], // actions when a non-printable key is pressed
	"onBeginLayer": [], // actions when the layer begins
	"onEndLayer": [], // actions when the layer ends
	"onComplete": [], // actions when complete
	"onMatch": [], // actions when a match is found
	"onNoMatch": [], // actions when no-match is found
	"onPartialMatch": [], // actions when a partial match is found
	"editable": false, // <bs> will remove the last key press. if no  keys are left it returns to the previous layer
	"actions": [ // list of keys - actions
		{ "keys": [], "actions":[], "name": ""} // an action entry with keys and the actions to take when pressed. Name is optional
	]
}
